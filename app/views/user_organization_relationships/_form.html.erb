<%= form_for(@user_organization_relationship) do |f| %>
  <% if @user_organization_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_organization_relationship.errors.count, "error") %> prohibited this user_organization_relationship from being saved:</h2>

      <ul>
      <% @user_organization_relationship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
